/**
 * A Space-Efficient Linked List
 * Instead of holding one element with two pointers we store a block(array) containing several items.
 * the SEList is parameterized by a block size (b) where each node is a block that holds b + 1
 *
 *
 * We need to do Deque operations add, remove, peek, resize
 */
public class MySEList {
}
